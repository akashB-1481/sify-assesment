apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-pv
spec:
  capacity:
    storage: 5Gi

  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle

  nfs:
    path: /data/k8s
    server: 192.168.16.199

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc
  namespace: db-ns
  labels:
    app: redis-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
