kind: Service
apiVersion: v1
metadata:
  name: nginx-service
  namespace: nginx-ns
spec:
  selector:
    app: nginx
  type: ClusterIP
  ports:
    - name: nginx-port
      port: 80
      targetPort: 80
