kind: Service
apiVersion: v1
metadata:
  name: nginx-service
  namespace: sify-assessment
spec:
  selector:
    app: nginx
  type: NodePort
  ports:
    - name: nginx-port
      port: 80
      targetPort: 8080
      nodePort: 30080
